# Copy this file to .env.local (for local dev) and set values

# TMDB
NEXT_PUBLIC_TMDB_API_KEY=
TMDB_READ_ACCESS_TOKEN=
NEXT_PUBLIC_TMDB_BASE_URL=https://api.themoviedb.org/3
NEXT_PUBLIC_TMDB_V4_BASE_URL=https://api.themoviedb.org/4

# Database (Vercel Postgres)
DATABASE_URL=

# NextAuth (later)
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# Redis (Upstash) - Optional but recommended for caching and rate limiting
# Get your Redis URL and token from: https://console.upstash.com/
# If not configured, features will work with graceful degradation (no caching, rate limiting allows all requests)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

